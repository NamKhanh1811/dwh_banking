my_dbt_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: airflow-postgresql
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: sor
      threads: 1