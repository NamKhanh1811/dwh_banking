my_dbt_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: airflow-postgresql
      user: airflow
      password: "{{ env_var('DBT_POSTGRES_PASSWORD') }}"
      port: 5432
      dbname: airflow
      schema: sor
      threads: 1
      sslmode: disable  